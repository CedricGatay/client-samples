plugins {
    id 'application'
    id 'scala'
    id 'org.openjfx.javafxplugin' version '0.0.8'
    id 'com.gluonhq.client-gradle-plugin' version '0.0.13'//5-SNAPSHOT'
}

repositories {
    mavenCentral()
    mavenLocal()
}

dependencies {
    compile 'org.scala-lang:scala-library:2.11.+'
    compileClasspath("com.oracle.substratevm:svm:19.2.0.1")
    implementation("fr.acinq.eclair:eclair-core_2.11:0.3.3-android")
}

gluonClient {
    //"--initialize-at-build-time",
    //  "--initialize-at-run-time=" +
    //    "akka.protobuf.DescriptorProtos," +
    //    "com.typesafe.config.impl.ConfigImpl$EnvVariablesHolder," +
    //    "com.typesafe.config.impl.ConfigImpl$SystemPropertiesHolder",
    //  "--no-fallback",
    //  "--allow-incomplete-classpath"
    verbose = true
    //reflectionList = []
   target = "ios-sim" // uncomment to deploy on iOS
   resourcesList = [".conf"]
   reflectionList = [ "akka.actor.LightArrayRevolverScheduler", "akka.dispatch.AbstractNodeQueue.Node", "akka.actor.LightArrayRevolverScheduler.TaskQueue" ]
   //graalPath = "/Users/cgatay/.sdkman/candidates/java/19.2.1-grl/"
}

javafx {
    version = "12.0.2"
    modules = [ "javafx.controls" ]
}

mainClassName = 'hellofx.HelloFX'
